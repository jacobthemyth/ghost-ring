{
  "name": "ghost-ring",
  "version": "0.0.0",
  "description": "One ring to rule them all",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "gulp": "^3.8.6",
    "gulp-shell": "^0.2.9",
    "gulp-directory-map": "0.0.1",
    "gulp-jade": "^0.7.0",
  }
}
